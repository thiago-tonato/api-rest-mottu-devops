spring:
  application:
    name: rastreamento

  datasource:
    url: jdbc:mysql://localhost:3306/mottu?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: mottuadmin
    password: QualiTracker123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    url: jdbc:mysql://localhost:3306/mottu?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    user: mottuadmin
    password: QualiTracker123!
    validate-on-migrate: false
    baseline-on-migrate: true

server:
  port: 8080
  address: 0.0.0.0
